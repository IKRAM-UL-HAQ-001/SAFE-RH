import 'package:flutter/material.dart';


class registerNewPatient extends StatefulWidget {
  const registerNewPatient({Key? key}) : super(key: key);

  @override
  State<registerNewPatient> createState() => _registerNewPatientState();
}

class _registerNewPatientState extends State<registerNewPatient> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
